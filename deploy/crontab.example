# Diamond scheduled jobs — install on the host with:  crontab deploy/crontab.example
# (or paste into `crontab -e`). CRON_TZ requires Vixie/cronie (Debian/Ubuntu default).
CRON_TZ=America/New_York
DIAMOND_DIR=/opt/diamond

# Morning full run: build today's slate + close out the prior day
# (final scores, stats, picks scoring, accuracy snapshot).
0 9 * * *        /opt/diamond/deploy/run-ingester.sh daily

# Afternoon quick loop (noon–6pm ET): confirmed lineups + odds + re-projection
# as lineups post. Matches the cadence documented in the README.
*/30 12-18 * * *  /opt/diamond/deploy/run-ingester.sh daily --quick

# Tennis full run — EARLY (5am ET): tennis sessions start overnight (AUS/Asia)
# and early morning (Europe), so build the tennis board ahead of the baseball run.
# (ratings -> slate -> project -> odds -> accuracy)
0 5 * * *        /opt/diamond/deploy/run-ingester.sh tennis-daily

# Tennis intraday refresh through the global playing day (slate + odds +
# re-projection). Hourly to bound Odds API per-event prop calls.
0 6-22 * * *     /opt/diamond/deploy/run-ingester.sh tennis-daily --quick

# Nightly database backup.
30 3 * * *       /opt/diamond/deploy/backup.sh
