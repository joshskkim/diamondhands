node_modules/
.env
.env.local
target/
.venv/
__pycache__/
.pytest_cache/
.pybaseball_cache/
*.iml
.idea/
# Ignore Claude Code local state but version the project skills (shared conventions).
.claude/*
!.claude/skills/
*.sqlite3
# Database dumps / backups — never commit (may hold real prod data). NB: not *.sql,
# which would swallow the tracked db/migrations/V*.sql files.
*.dump
*.sql.gz
